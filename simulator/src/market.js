const { pool } = require("./db.js");

// TODO: wait for Martin branch before implementing
function sellToMarket(amount) {}

// TODO: wait for Martin branch before implementing
function buyFromMarket(amount) {}

module.exports = { sellToMarket, buyFromMarket };
